apiVersion: apps/v1
kind: Deployment
metadata:
  name: simplesim-frontend
  namespace: simplesim
  labels:
    app: simplesim-frontend
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simplesim-frontend
  template:
    metadata:
      labels:
        app: simplesim-frontend
        version: v1
    spec:
      effect: NoSchedule
      containers:
        - name: frontend
          image: 723216754178.dkr.ecr.eu-west-1.amazonaws.com/simplesim-frontend:${CI_COMMIT_SHORT_SHA}
          ports:
            - containerPort: 3000
              name: http
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
