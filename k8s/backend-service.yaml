apiVersion: v1
kind: Service
metadata:
  name: simplesim-backend
  namespace: simplesim
  labels:
    app: simplesim-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: simplesim-backend 